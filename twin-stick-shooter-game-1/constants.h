#pragma once
#include <math.h>

const int TICK_CALLBACK = 10;
//I normally do 20 but I'll need higher precision collisions
//16ms = 60Hz, 20ms = 50Hz, 10ms = 100Hz

const float GAME_WIDTH = 1000;
const float GAME_HEIGHT = 1000;

const double PI = M_PI;

